import React from "react";
import AppLayout from "../AppLayout";

interface Props {}

export default function Forum(props: Props) {
  return (
    <AppLayout>
      <div>Forum</div>
    </AppLayout>
  );
}
